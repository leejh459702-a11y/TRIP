# 여행 코스 · 기록 앱 — 구현 명세서
> 이 문서는 Claude Code에게 전달하는 구현 지시서입니다.
> 처음부터 끝까지 읽고, **Phase 0부터 순서대로** 진행하세요.
> 각 Phase가 끝날 때마다 동작 확인 후 커밋하고, 다음으로 넘어갑니다.
>
> **[다음 작업자 참고]** 이 문서는 프로젝트 시작 시 원저자가 작성한 원본 명세서 원문입니다(수정 없이 그대로 보존).
> 현재까지의 구현 현황·미구현 항목·다음 할 일은 `HANDOFF.md`를 먼저 읽으세요.
---
## 0. 프로젝트 정의
### 무엇을 만드는가
개인용 **여행 코스 설계 + 방문 기록** 앱. 상업 서비스가 아니라 제작자 1인이 직접 쓰는 도구입니다.
두 개의 축이 하나의 루프를 이룹니다.
```
장소 저장 → 코스 설계 → 현장 실행 → 경험 기록
     ↑                                    │
     └──────── 재방문 판정 ────────────────┘
```
### 핵심 목적
1. **일정을 알아보기 쉽게 만든다** — 짜는 것보다 읽히는 것이 중요합니다. 타임라인 뷰가 이 앱의 정체성입니다.
2. **경험을 기록해 자산으로 쌓는다** — 기록이 다음 여행의 재료가 되어야 합니다.
3. **소중한 사람을 안내한다** — 앱 없는 사람에게 코스를 보여줄 수 있어야 합니다.
### 만들지 않는 것
- 콘텐츠 제작 기능(촬영 체크리스트, 업로드 관리 등) — 범위 밖
- 자체 내비게이션 — 외부 앱으로 넘깁니다
- 소셜, 팔로우, 공개 피드 — 개인 도구입니다
- 사진 자동 매칭(EXIF 스캔) — 이번 범위 제외. 수동 업로드만
---
## 1. 기술 스택
| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | React 18 + TypeScript | strict 모드 |
| 빌드 | Vite | |
| 백엔드 | Firebase (Firestore, Auth, Storage) | 익명 로그인 또는 Google |
| 지도 | **카카오맵 Web(JavaScript) SDK** | |
| 장소/경로 데이터 | **카카오맵 REST API + 카카오내비 REST API** | 3절 참조 |
| 드래그앤드롭 | **dnd-kit** | `react-beautiful-dnd` 사용 금지 (유지보수 중단) |
| 상태 관리 | Zustand | Redux 쓰지 말 것 |
| 라우팅 | React Router v6 | |
| 폼/검증 | react-hook-form + zod | |
| 날짜 | date-fns | moment 금지 |
| 배포 | PWA (vite-plugin-pwa) | 오프라인 캐시 필요 |
### 왜 카카오인가 (변경 금지 근거)
네이버는 대중교통·도보 경로 API를 제공하지 않습니다. 카카오는 2026년 7월 21일부터 대중교통/도보/자전거 경로 API를 열었습니다. "이동수단별 동선·시간 확인"이 이 앱의 필수 요구사항이므로 카카오가 유일한 선택지입니다.
단, **경로 조회는 반드시 인터페이스로 추상화**합니다(H1). 네이버가 대중교통 API를 열면 갈아끼울 수 있어야 합니다.
---
## 2. 디자인 방향
### 원칙
이 앱의 화면은 **지도 위에 얹히는 UI**입니다. UI가 지도를 이기면 안 됩니다. 채도 높은 배경, 큰 그림자, 화려한 카드를 쓰지 마세요.
색은 장식이 아니라 **분류 정보**입니다. 카테고리 색은 지도 핀 · 블록 카드 · 타임라인에서 동일하게 유지되어야 합니다.
### 토큰
```css
/* 표면 */
--canvas:     #EDEEEA;   /* 앱 배경 */
--surface:    #FFFFFF;   /* 카드, 시트 */
--surface-2:  #F6F7F4;   /* 비활성 영역 */
--line:       #DDDFD8;   /* 구분선 */
/* 텍스트 */
--ink:        #1C1F1A;   /* 본문 */
--ink-muted:  #6E7268;   /* 보조 정보 */
/* 카테고리 (지도 핀과 동일하게 사용) */
--cat-food:      #C4562F;
--cat-cafe:      #A07B3E;
--cat-stay:      #3D6B8C;
--cat-activity:  #5E8C4A;
--cat-etc:       #8A7B9B;
/* 상태 */
--warn:       #B3341F;   /* 영업시간 충돌, 이동 이상 */
--ok:         #3F7A4F;
--free:       #9BA096;   /* 자유시간 블록 */
```
`#F4F1EA` 계열 크림 배경, `#D97757` 계열 테라코타 액센트는 쓰지 마세요.
### 타이포그래피
**Pretendard Variable 한 패밀리**만 씁니다. 위계는 weight와 size로만 만듭니다.
가장 중요한 규칙: **타임라인의 시각 컬럼은 `font-variant-numeric: tabular-nums` 필수.** 숫자 폭이 흔들리면 시간축이 어긋나 보이고, 그러면 이 앱의 존재 이유가 무너집니다. 소요시간·거리·비용 등 모든 숫자에 동일 적용.
```css
--font: 'Pretendard Variable', Pretendard, -apple-system, sans-serif;
--num:  { font-variant-numeric: tabular-nums; font-feature-settings: 'tnum'; }
```
라벨에 대문자(ALL CAPS)를 쓰지 마세요. 한글 앱입니다. 제목 위에 작은 영문 eyebrow 라벨을 붙이지 마세요.
### 모션
사용자 동작에 대한 응답(블록 드래그, 시트 열기, 뷰 전환)만 애니메이션합니다. 스크롤 진입 시 fade-up, 카드 hover transition은 넣지 마세요.
### 카피
- 버튼은 동작 결과를 그대로 씁니다. "제출"이 아니라 "코스 저장"
- 빈 화면은 안내가 아니라 **행동 유도**입니다. "저장된 장소가 없습니다"가 아니라 "지도에서 마음에 든 곳을 저장해 보세요"
- 에러는 사과하지 않고 무엇이 잘못됐고 어떻게 고치는지 말합니다
---
## 3. 카카오 API 연동
### 사용 API 목록
| 용도 | API | 엔드포인트 | 무료/일 | 초과 단가 |
|---|---|---|---|---|
| 지도 표시 | 지도 Web SDK | (SDK) | 300,000 | 0.1원 |
| 장소 검색 | 키워드로 장소 검색 | `dapi.kakao.com/v2/local/search/keyword` | 100,000 | 2원 |
| 카테고리 검색 | 카테고리로 장소 검색 | `dapi.kakao.com/v2/local/search/category` | 100,000 | 2원 |
| **지역 자동분류** | 좌표로 행정구역정보 변환 | `dapi.kakao.com/v2/local/geo/coord2regioncode` | 100,000 | 0.5원 |
| 주소 표시 | 좌표로 주소 변환 | `dapi.kakao.com/v2/local/geo/coord2address` | 100,000 | 0.5원 |
| **대중교통 경로** | 대중교통 경로 조회 | `dapi.kakao.com/v2/routing/publictraffic` | **1,000** | 10원 |
| **도보 경로** | 도보 경로 조회 | `dapi.kakao.com/v2/routing/walk` | **1,000** | 10원 |
| 공유 썸네일 | 정적 지도 조회 | `dapi.kakao.com/v2/maps/staticmap` | **1,000** | 2원 |
| **자동차 다구간** | 다중 경유지 길찾기 (카카오내비) | `developers.kakaomobility.com` 문서 참조 | 5,000 | — |
| 자동차 단일구간 | 자동차 길찾기 (카카오내비) | 동일 | 10,000 | — |
전체 API 월간 무료 쿼터는 3,000,000건입니다.
### 쿼터 관리 (H2 — 필수)
**대중교통·도보·정적지도는 일 1,000건뿐입니다.** 캐싱 없이 개발하면 하루 만에 소진됩니다.
구현 규칙:
1. 모든 경로 조회 결과는 Firestore `legs` 컬렉션에 캐싱합니다.
2. 캐시 키: `${fromLat},${fromLng}_${toLat},${toLng}_${mode}` (좌표는 소수점 5자리로 반올림)
3. TTL: 자동차 **1시간**(실시간 교통 반영), 대중교통·도보 **7일**(잘 안 변함)
4. 블록 순서가 바뀌어도 **변경된 구간만** 재조회합니다. 전체 재계산 금지
5. 드래그 중에는 조회하지 않습니다. 드롭 완료 후 300ms 디바운스
6. 개발 중에는 `.env`의 `VITE_USE_MOCK_ROUTING=true`로 목 데이터를 씁니다
### 키 종류 주의
- 지도 Web SDK → **JavaScript 키** (+ 콘솔에 도메인 등록)
- REST API → **REST API 키**
- 둘을 바꿔 쓰면 에러가 납니다.
### 앱 등록 순서 주의
카카오맵 API 무료 쿼터는 **개발자 계정 기준 첫 번째로 활성화한 앱에만** 제공됩니다. 두 번째 앱부터는 비즈월렛 연결 + 유료 설정이 필요합니다. 이 앱을 첫 번째로 등록하세요.
### 없는 것
**영업시간 API는 어디에도 없습니다.** 수동 입력 + 외부 플레이스 링크로 처리합니다(B1 참조).
### 경로 API 추상화 (H1 — 최우선 구현)
```ts
// src/services/routing/types.ts
export type TravelMode = 'car' | 'transit' | 'walk';
export interface RouteLeg {
  durationMin: number;
  distanceM: number;
  mode: TravelMode;
  transitSummary?: string;   // "2호선 → 도보 8분"
  tollFee?: number;
  fetchedAt: number;
}
export interface RoutingProvider {
  name: string;
  getLeg(from: LatLng, to: LatLng, mode: TravelMode): Promise<RouteLeg>;
  getMultiStopCar(points: LatLng[]): Promise<RouteLeg[]>;
  supports(mode: TravelMode): boolean;
}
```
`KakaoRoutingProvider`와 `MockRoutingProvider` 두 개를 구현하고, 애플리케이션 코드는 인터페이스만 참조합니다. 컴포넌트에서 `dapi.kakao.com`을 직접 호출하는 코드가 하나라도 있으면 잘못된 구현입니다.
---
## 4. 데이터 모델
```ts
// ─── 장소 ────────────────────────────────────────────
export type Category = 'food' | 'cafe' | 'stay' | 'activity' | 'etc';
export interface Place {
  id: string;
  name: string;
  category: Category;
  lat: number;
  lng: number;
  address: string;
  kakaoPlaceId?: string;
  placeUrl?: string;              // 카카오/네이버 플레이스 링크
  region: RegionCode;             // coord2regioncode로 자동 채움
  tags: string[];                 // 상황 태그 (수동)
  businessHours?: BusinessHours;  // 수동 입력
  defaultStayMin: number;         // 카테고리별 기본값에서 시작
  estCostPerPerson?: number;      // B10
  visitCount: number;             // 파생 필드, 방문 기록 시 갱신
  lastVisitedAt?: string;
  latestRevisit?: Revisit;        // E1 소환용 캐시
  createdAt: string;
  updatedAt: string;
}
export interface RegionCode {
  sido: string;      // "강원특별자치도"
  sigungu: string;   // "강릉시"
  dong?: string;     // "초당동"
}
export interface BusinessHours {
  weekly: DayHours[];             // 길이 7, 0=일요일
  lastOrderMin?: number;          // 마감 몇 분 전 라스트오더
  note?: string;                  // "매월 2·4주 화요일 휴무"
}
export interface DayHours {
  closed: boolean;
  open?: string;                  // "11:00"
  close?: string;                 // "21:00"
  breakStart?: string;            // "15:00"
  breakEnd?: string;              // "17:00"
}
// ─── 방문 기록 ────────────────────────────────────────
export type Revisit = 'yes' | 'maybe' | 'no';
export interface Visit {
  id: string;
  placeId: string;
  courseId?: string;
  visitedAt: string;              // ISO
  revisit: Revisit;
  companions: string[];           // ['부모님', '연인', ...]
  memo?: string;                  // 한 줄
  photoUrls?: string[];           // 수동 업로드만
  voiceMemoUrl?: string;          // D6
  cost?: number;                  // B10 대조용
  auto: VisitAuto;                // D4
}
export interface VisitAuto {
  weekday: number;
  timeSlot: 'morning' | 'lunch' | 'afternoon' | 'evening' | 'night';
  season: 'spring' | 'summer' | 'autumn' | 'winter';
  weather?: string;               // B7과 동일 소스
  stayMin?: number;               // 실제 체류 (C4 체크오프 간격)
  partySize?: number;
}
// ─── 코스 ─────────────────────────────────────────────
export interface Course {
  id: string;
  title: string;
  startDate: string;
  partySize: number;
  days: CourseDay[];              // B9
  isTemplate: boolean;            // B8
  shareToken?: string;            // F1
  createdAt: string;
  updatedAt: string;
}
export interface CourseDay {
  id: string;
  date: string;
  startTime: string;              // "09:00"
  anchorPlaceId?: string;         // 숙소 (B9)
  blocks: Block[];
}
export interface Block {
  id: string;
  type: 'place' | 'free';
  placeId?: string;               // type === 'place'
  label?: string;                 // type === 'free' ("점심 후 휴식")
  stayMin: number;
  modeToNext?: TravelMode;        // 다음 블록까지 이동수단
  planBPlaceIds?: string[];       // B3
  estCost?: number;               // B10
  done?: boolean;                 // C4
  doneAt?: string;
  delayMin?: number;              // C2 (누적 지연)
}
// ─── 파생 (저장하지 않음, 계산) ───────────────────────
export interface TimelineEntry {
  block: Block;
  arriveAt: Date;
  leaveAt: Date;
  legToNext?: RouteLeg;
  warnings: BlockWarning[];
}
export type BlockWarning =
  | { kind: 'closed';        detail: string }   // B1 정기휴무
  | { kind: 'breaktime';     detail: string }   // B1 브레이크타임
  | { kind: 'lastOrder';     detail: string }   // B1 라스트오더
  | { kind: 'longTransfer';  detail: string }   // B2 이동 40분+
  | { kind: 'badWeather';    detail: string };  // B7
```
### Firestore 구조
```
users/{uid}/places/{placeId}
users/{uid}/visits/{visitId}
users/{uid}/courses/{courseId}
users/{uid}/legs/{cacheKey}          ← 경로 캐시 (H2)
shared/{shareToken}                  ← F1 공개 읽기 전용 스냅샷
```
`shared`만 인증 없이 읽기 가능. 나머지는 본인만.
---
## 5. 화면 구조
```
[하단 탭 — 5개 고정]
🗺  지도      저장 장소 전체 · 카테고리/태그/지역 필터 · 클러스터링
📋  일정      코스 목록 → 빌더 / 타임라인 / 지도
🔄  재방문    또감 리스트 · 보류함 · 시기 리마인드
📖  기록      타임라인 · 히트맵 · 연간 리포트
👤  마이      태그 관리 · 데이터 내보내기 · 설정
```
### 코스 화면의 3단 뷰 (가장 중요)
상단 세그먼트 컨트롤로 전환합니다. **편집 모드와 확인 모드를 분리하는 것이 핵심입니다.**
| 뷰 | 용도 | 구성 |
|---|---|---|
| **블록** | 편집 | 드래그앤드롭 리스트 + 지도 (편집 가능) |
| **타임라인** | 파악 | 세로 시간축, 읽기 전용 |
| **지도** | 동선 | 순서 번호 핀 + 경로선 |
레이아웃:
```
데스크톱  좌 40% 블록 리스트 | 우 60% 지도
모바일    상단 지도 45% / 하단 리스트 55%, 바텀시트로 비율 조절
```
### 타임라인 뷰 렌더링 규칙
```
09:00 ┃ 🏨 숙소 출발
      ┋  🚗 22분
09:22 ┃ ☕ ○○카페            60분
      ┃    "창가석 명당"                    ← E1 지난 메모
      ┋  🚶 8분
10:30 ┃ 🏛 ○○미술관          90분
      ┋  🚗 15분
12:15 ┃ 🍜 ○○식당            80분
      ┃    ⚠️ 브레이크타임 15:00 시작        ← B1
      ┋  🚗 35분  ⚠️ 이동 김                ← B2
13:50 ┃ 🌊 ○○해변            60분
─────────────────────────────────────
총 이동 80분 · 체류 290분 · 5곳 · 약 8만원
빡빡함: ●●●○○ 적당
```
- **블록 세로 높이 = 실제 체류시간에 비례** (`1분 = 0.8px`, 최소 44px). 하루가 얼마나 빡빡한지 눈으로 보여야 합니다
- 체류는 실선(┃), 이동은 점선(┋). 이동 시간이 길수록 점선이 길어집니다
- 왼쪽 시각 컬럼은 고정폭 + tabular-nums
- 경고는 해당 블록에 인라인으로
---
## 6. 기능 명세
각 항목의 ID는 그대로 유지하세요. 커밋 메시지에 `feat(B1): ...` 형태로 참조합니다.
### A. 입력 편의
**A6 — 일괄 태그 편집**
장소 목록에서 다중 선택 모드 진입 → 여러 장소에 태그를 한 번에 추가/제거. 지도 탭과 재방문 탭 양쪽에서 동작해야 합니다.
---
### B. 코스 설계
**B1 — 영업시간 충돌 경고 ⭐**
가장 중요한 기능입니다. 이 앱이 존재하는 이유 중 하나입니다.
- 장소마다 영업시간을 수동 입력하는 폼 제공 (요일별 open/close/break, 정기휴무, 라스트오더)
- 타임라인 계산 시 각 블록의 도착 예정 시각과 대조
- 경고 3종: `정기휴무` / `브레이크타임에 도착` / `라스트오더 이후 도착`
- 영업시간이 입력되지 않은 장소는 회색 "영업시간 미확인" 배지 + 플레이스 링크 버튼
- 경고는 무시 가능하되, 무시해도 배지는 남습니다
**B2 — 이동 이상 감지**
구간 이동시간이 임계값(기본 40분, 설정에서 조절)을 넘으면 해당 leg를 `--warn` 색으로 표시하고 "이동 김" 라벨. 하루 총 이동시간이 총 체류시간을 넘으면 상단에 별도 배너.
**B3 — Plan B 슬롯**
블록 카드를 펼치면 대체 후보 목록(최대 3개). 후보는 저장된 장소에서 고릅니다. "여기로 교체" 한 번에 스왑되고 경로가 재계산됩니다. 후보 선택 시 현재 블록과 가까운 순으로 정렬해 추천.
**B4 — 자유시간 블록**
`type: 'free'` 블록. 장소 없이 시간만 차지합니다. 라벨 자유 입력. 지도에 핀이 찍히지 않고 경로 계산에서 제외되며, 앞뒤 블록이 직접 연결됩니다.
**B5 — 빡빡함 게이지**
5단계 표시. 산출식:
```
density = (총이동분 / 총소요분) * 0.5
        + (블록수 / 6) * 0.3
        + (여유분 < 0 ? 0.2 : 0)
```
- 하루 place 블록이 5개를 넘으면 부드러운 안내 표시: "하루 5곳을 넘으면 대부분 못 지킵니다"
- 강제로 막지 말 것. 안내만 합니다
**B6 — 순서 자동 최적화**
`B1, B2가 구현된 뒤에 착수합니다.` 시간 제약 없는 최적화는 영업시간을 무시한 코스를 만들어 쓸모가 없습니다.
- 식사 블록(`food`)은 시간대 고정: 점심 11:30~13:30, 저녁 17:30~19:30 창 안에 유지
- 숙소(`stay`)는 하루의 마지막 또는 첫 블록으로 고정
- 나머지 블록만 이동시간 최소화로 재배치 (블록 6개 이하면 완전탐색, 초과면 nearest-neighbor + 2-opt)
- 실행 전 "이렇게 바뀝니다" 미리보기 → 사용자 승인 후 적용, 되돌리기 제공
**B7 — 날씨 연동**
Open-Meteo API 사용(무료, 키 불필요). 코스 날짜·좌표 기준 예보 조회.
- 강수확률 60% 이상이면 야외 성격 블록(`activity`, 태그 `야외`)에 우산 아이콘 + 경고
- "실내 대안 보기" → 같은 지역의 실내 카테고리 저장 장소를 Plan B 후보로 제안
- 7일 이후 날짜는 예보 없음. 조용히 생략합니다
**B8 — 코스 템플릿**
`isTemplate: true`로 저장. 새 코스 만들 때 템플릿에서 시작하면 블록 구조·체류시간·이동수단은 유지되고 장소만 비어 있습니다. "이 자리에 넣을 장소" 슬롯으로 표시.
**B9 — 다일정 관리**
`days[]` 배열. 날짜 탭으로 전환하고, 보지 않는 날은 접힙니다.
- 각 날의 `anchorPlaceId`(숙소)를 지정하면 그날의 시작·끝이 숙소로 자동 고정
- 날짜 간 블록 이동 지원 (dnd-kit 멀티 컨테이너)
- 전체 일정 요약 뷰: 날짜별 이동/체류/장소수 한 줄 요약
**B10 — 예산 추정**
블록별 `estCost` 입력(장소의 `estCostPerPerson × partySize`가 기본값). 하루 합계 + 전체 합계 표시. 방문 기록의 실제 `cost`가 있으면 나중에 "예상 vs 실제" 대조를 보여줍니다.
---
### C. 현장 실행
**C1 — 오늘의 코스 라이브 뷰 ⭐**
오늘 날짜의 코스가 있으면 앱 진입 시 이 화면이 먼저 뜹니다.
- 이동 중 한 손으로 보는 화면입니다. 큰 글씨, 최소 정보
- 구성: `지금 어디` (현재 블록, 크게) / `다음 어디` / `몇 시에 출발해야 하는지` / 큰 버튼 2개 = `[여기 완료]` `[길찾기]`
- 스크롤 없이 한 화면에 들어와야 합니다
- 지금 블록이 없으면(이동 중) 다음 블록까지 남은 시간 카운트다운
**C2 — 지연 반영**
"n분 늦음" 입력(±5분 스텝퍼 또는 현재 시각 기준 자동 계산). 이후 모든 블록의 도착 예정 시각이 재계산되고 **B1 영업시간 검증이 다시 돌아갑니다.** 지연으로 새로 발생한 경고는 강조 표시.
**C3 — 오프라인 캐시**
서비스워커로 오늘·내일 코스 데이터와 관련 장소·leg를 미리 캐싱. 오프라인 상태에서도 타임라인 조회, 블록 체크오프, 방문 기록 작성이 가능해야 합니다. 온라인 복귀 시 큐에 쌓인 쓰기를 동기화. Firestore 오프라인 persistence 활성화.
**C4 — 블록 체크오프**
블록을 완료 처리하면 `done: true`, `doneAt` 기록. 완료 시 방문 기록 입력 시트가 바로 올라옵니다(E·D 연결점). 스킵 가능하되 스킵하면 미기록 배지가 남습니다. `doneAt` 간격으로 실제 체류시간(`VisitAuto.stayMin`)을 자동 산출.
**C5 — 다음 장소 알림**
출발해야 할 시각 10분 전 로컬 알림. 알림 내용은 "○○로 출발 · 🚗 22분". 설정에서 on/off 및 리드타임 조절. PWA Notification API 사용, 권한 거부 시 조용히 비활성화.
---
### D. 기록
**D1 — 방문 히트맵**
전국 지도에 방문 장소를 밀도로 표시. 시군구 단위 채색(방문 수에 비례). 안 가본 지역이 시각적으로 드러나는 것이 목적입니다. 시군구 경계는 공공데이터 GeoJSON을 정적 파일로 포함하거나, 없으면 좌표 클러스터링으로 대체.
**D2 — 기록 타임라인**
시간순 아카이브. 3가지 정렬 축을 제공합니다.
- 시간순: `2026년 3월 › 강릉 1박2일 › 6곳`
- 장소순: 한 장소의 모든 방문 이력
- 지역순: 시도 › 시군구 트리
**D4 — 자동 메타 기록**
방문 기록 시 사용자 입력 없이 자동 저장: 날짜, 요일, 시간대(아침/점심/오후/저녁/밤), 계절, 그날 그 지역 날씨(B7과 동일 소스), 실제 체류시간(C4), 동행 인원(코스의 `partySize`). 기록 화면에서 조용히 부가정보로 표시할 뿐, 입력 부담을 주지 않습니다.
**D5 — 연간 리포트**
연말 또는 임의 시점에 요약 카드 생성.
```
올해 47곳 방문 · 12개 시군구 · 재방문 8곳
가장 많이 간 곳: ○○식당 (3회)
가장 자주 함께한 사람: 부모님 (14회)
새로 가본 지역: 강릉, 통영, 목포
```
이미지로 저장 가능(F3와 렌더러 공유).
**D6 — 음성 메모**
MediaRecorder API로 녹음 → Firebase Storage 업로드. 방문 기록에 첨부. 이동 중 손이 자유롭지 않을 때 씁니다. 최대 60초 제한. 자동 전사(STT)는 이번 범위 제외.
---
### E. 재방문
**E1 — 재방문 소환 ⭐**
코스에 장소를 추가하는 순간, 그 장소의 마지막 방문 기록이 블록 카드에 자동 표시됩니다.
```
ⓘ 지난 방문 2025.11 · 친구 3인 · 체류 95분
  "웨이팅 40분"
```
기록한 것을 다시 읽게 만드는 유일한 장치입니다. 이게 없으면 기록할 이유가 없습니다.
**E2 — 방문 회차 누적**
같은 장소를 다시 방문해도 새 카드가 생기지 않습니다. 기존 장소 카드에 회차가 쌓입니다.
```
[스시 ○○]  방문 3회
  3차 · 2026.05.12 · 연인 · "오마카세로 바꿈, 더 좋아짐"
  2차 · 2025.11.03 · 친구 3인 · "웨이팅 40분"
  1차 · 2025.06.20 · 혼자 · "런치 가성비 좋음"
```
`revisit` 판정이 회차마다 다를 수 있습니다. 최신 판정을 대표값으로 씁니다.
**E3 — 보류함**
`revisit: 'maybe'` 장소들의 별도 서랍. 재방문 탭 안의 하위 섹션. "또 감"으로 승격하거나 "안 감"으로 정리하는 액션 제공.
**E4 — 시기 리마인드**
계절성이 있는 장소를 놓치지 않게 합니다.
- 조건: 작년 같은 시기(±2주)에 방문 기록이 있고 `revisit: 'yes'`인 장소
- 표시: 홈 상단 카드 (푸시 알림 아님)
- "작년 이맘때 갔던 ○○, 지금이 그 시즌입니다"
**E5 — 경과 알림**
마지막 방문 후 일정 기간(기본 12개월) 지난 `revisit: 'yes'` 장소에 "1년 경과" 배지. 재방문 리스트 정렬 옵션에 "오래된 순" 추가.
---
### F. 공유
**F1 — 앱 없이 보는 링크 ⭐**
"소중한 사람을 안내한다"는 목적의 직접 구현입니다.
- 코스를 `shared/{shareToken}`에 읽기 전용 스냅샷으로 복사 (원본 참조 아님, 값 복사)
- 공개 라우트 `/s/:token` — 로그인 불필요, 설치 불필요
- 내용: 지도 + 순서 핀 + 시간표 + 각 장소의 외부 지도 링크
- **포함하지 않는 것**: 개인 메모, 재방문 판정, 예산, 지난 방문 기록
- 링크 만료 설정(기본 90일) 및 즉시 해제 제공
- 모바일 웹에서 잘 보여야 합니다. 이 화면은 대부분 카톡 인앱 브라우저에서 열립니다
**F2 — 카카오톡 공유 카드**
카카오톡 공유 API(JavaScript SDK)로 F1 링크를 카드 형태로 전송. 썸네일은 정적 지도 조회 API로 생성한 경로 이미지. 제목/설명은 코스 제목 + 요약.
**F3 — 코스 이미지 저장**
타임라인 뷰를 이미지로 렌더링(`html-to-image` 또는 canvas 직접 그리기). 세로 긴 형태. 톡방에 바로 올릴 수 있게. D5 연간 리포트와 렌더러를 공유합니다.
**F4 — 동행자 코멘트**
F1 공유 링크에서 익명 반응. 블록마다 👍 또는 짧은 코멘트(30자). 로그인 없이 브라우저 로컬 식별자로 중복만 방지. 원본 소유자에게만 보입니다. **가장 후순위로 두세요.** 없어도 앱이 성립합니다.
**F5 — 캘린더 내보내기**
코스를 `.ics` 파일로 생성. 블록당 하나의 VEVENT, 위치는 좌표+주소, 설명에 이동수단·소요시간. 기본 캘린더 앱에 넣으면 알림을 캘린더가 대신 처리해 줍니다.
---
### G. 탐색
**G1 — 상황 기반 검색**
태그 조합 필터. `부모님` + `강릉` + `주차편함` 같은 다중 조건 AND 검색. 지역은 `RegionCode`로 자동 필터, 상황 태그는 수동 태그로 필터. 자주 쓰는 조합을 저장할 수 있게 합니다.
태그 체계(초기값, 사용자가 추가 가능):
```
동행  부모님 · 연인 · 친구 · 혼자 · 아이동반
상황  비오는날 · 주차편함 · 조용함 · 사진잘나옴 · 웨이팅있음 · 예약필수
검증  가봄 · 가보고싶음 · 추천받음
```
**G2 — 주변 저장 장소 알림**
Geolocation watch. 저장된 장소 500m 이내 진입 시 알림. 배터리를 고려해 저전력 모드 사용, 설정에서 기본 off. 같은 장소 알림은 24시간에 1회만.
**G3 — 미방문 리스트**
저장했지만 방문 기록이 없는 장소 모음. 저장 후 경과일 순 정렬. "이만 정리하기" 액션으로 일괄 삭제 가능(A6과 연결).
**G4 — 코스 초안 자동 생성**
`방문 기록이 최소 30건 쌓인 뒤에 의미가 생깁니다. 그 전까지는 UI에 노출하되 "기록이 더 필요합니다" 안내를 띄우세요.`
입력: 지역 / 동행 / 인원 / 일수 / 예산대
처리: 저장 장소 중 조건에 맞고 `revisit: 'yes'`인 것을 우선 선정 → 카테고리 배분(끼니 수 = 일수 × 2) → 지리적 근접도로 순서 배치 → 영업시간 검증(B1) → 초안 코스 생성
**외부 추천을 절대 섞지 마세요.** 내가 검증한 장소만으로 만들어지는 것이 이 기능의 핵심 가치입니다.
**G5 — 지도 클러스터링**
장소가 많아지면 줌 레벨에 따라 묶어서 표시. 카카오맵 SDK의 MarkerClusterer 사용. 클러스터 색은 포함된 장소의 최빈 카테고리 색으로.
---
### H. 기반
**H1 — 경로 API 추상화 ⭐**
3절의 `RoutingProvider` 인터페이스. **Phase 0에서 가장 먼저 구현합니다.** 나중에 하려고 하면 못 합니다.
**H2 — API 응답 캐싱**
3절의 쿼터 관리 규칙 그대로. 추가로 개발자용 쿼터 사용량 표시를 마이 탭에 넣으세요(오늘 호출 수 / 무료 한도).
**H3 — 데이터 내보내기**
전체 데이터를 JSON으로 다운로드. 장소·방문·코스 전부. 가져오기도 지원해서 왕복이 되어야 합니다. 장소·방문은 CSV 별도 제공.
**H4 — PWA 오프라인**
`vite-plugin-pwa`. 앱 셸 프리캐시 + 지도 타일 런타임 캐시(용량 제한) + Firestore persistence. 홈 화면 추가 가능. C3와 함께 구현합니다.
**H5 — 다크모드**
`prefers-color-scheme` 자동 + 수동 토글. 2절 토큰의 다크 버전을 정의하되, **카테고리 색은 채도를 낮추지 말고 명도만 조정**하세요. 분류 정보가 흐려지면 안 됩니다. 지도 SDK도 다크 스타일로 전환.
---
## 7. 구현 순서
각 Phase 끝에서 실행 가능한 상태여야 합니다.
### Phase 0 — 기반 (먼저 끝내야 나머지가 안 꼬임)
- 프로젝트 셋업, Firebase 연결, 인증
- 데이터 모델 + Firestore 스키마 + 보안 규칙
- **H1 경로 추상화 + MockRoutingProvider**
- **H2 캐싱 레이어**
- 디자인 토큰, 기본 컴포넌트, 5탭 셸
### Phase 1 — 코어 루프
- 카카오 지도 표시, 장소 검색·저장
- `coord2regioncode` 지역 자동 분류
- 코스 빌더 드래그앤드롭(dnd-kit)
- 체류시간 슬롯, **타임라인 뷰**, 3단 뷰 전환
- KakaoRoutingProvider 실연결 (car → transit → walk 순)
- 외부 지도 딥링크 (네이버/카카오/티맵 선택)
### Phase 2 — 경고와 기록
- **B1 영업시간** (입력 폼 + 충돌 검증)
- B2 이동 이상, B4 자유시간, B5 빡빡함
- **C4 체크오프 → 방문 기록 3항목**
- **E1 재방문 소환**, E2 회차 누적, E3 보류함
- D4 자동 메타
### Phase 3 — 실행과 공유
- **C1 라이브 뷰**, C2 지연 반영
- **C3 + H4 오프라인 PWA**
- C5 알림
- **F1 공유 링크**, F2 톡 공유, F5 캘린더
- B9 다일정, B3 Plan B
### Phase 4 — 확장
- B7 날씨, B8 템플릿, B10 예산
- D1 히트맵, D2 타임라인, D5 리포트, D6 음성
- E4 시기 리마인드, E5 경과
- G1 상황 검색, G3 미방문, G5 클러스터링
- A6 일괄 태그, H3 내보내기, H5 다크모드
### Phase 5 — 데이터가 쌓인 뒤
- B6 자동 최적화
- G2 주변 알림
- G4 코스 자동 생성
- F4 동행자 코멘트
---
## 8. 코딩 규칙
- TypeScript strict. `any` 금지. 불가피하면 `unknown` + 타입 가드
- 컴포넌트 파일 200줄 넘으면 분리
- 비즈니스 로직은 `src/domain/`에 순수 함수로. 타임라인 계산, 영업시간 검증, 빡빡함 산출은 React와 무관하게 테스트 가능해야 합니다
- 타임라인 계산과 영업시간 검증은 **단위 테스트 필수** (vitest). 이 두 개가 틀리면 앱 전체가 거짓말을 합니다
- 시간 계산은 전부 `date-fns`. 직접 `Date` 산술 금지
- 좌표 비교는 소수점 5자리 반올림 후 비교
- API 키는 `.env`. 커밋 금지. `.env.example` 제공
- 커밋 메시지: `feat(B1): 브레이크타임 충돌 검증 추가`
### 환경변수
```
VITE_KAKAO_JS_KEY=
VITE_KAKAO_REST_KEY=
VITE_KAKAO_MOBILITY_KEY=
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_APP_ID=
VITE_USE_MOCK_ROUTING=true
```
REST 키가 클라이언트에 노출되는 것을 피하려면 Firebase Functions로 프록시하세요. 개인용이면 Phase 1에서는 직접 호출해도 되지만, 도메인 제한은 반드시 걸어 두세요.
---
## 9. 하지 말 것
- `localStorage` / `sessionStorage`를 상태 저장소로 쓰지 마세요. Firestore + Zustand 사용
- 컴포넌트에서 `dapi.kakao.com`을 직접 호출하지 마세요. H1 위반입니다
- 드래그 중에 경로 API를 호출하지 마세요. 쿼터가 즉시 터집니다
- 영업시간을 크롤링하지 마세요. 수동 입력입니다
- B6을 B1·B2보다 먼저 만들지 마세요
- G4를 데이터 없이 만들지 마세요
- 타임라인 뷰를 편집 가능하게 만들지 마세요. 읽기 전용이 존재 이유입니다
- 5개 탭을 늘리지 마세요
---
## 10. 완료 판정
Phase 1이 끝났을 때 이게 되어야 합니다:
1. 지도에서 "강릉 카페" 검색 → 결과 3개 저장
2. 저장된 장소가 `강원특별자치도 › 강릉시`로 자동 분류됨
3. 새 코스 만들고 4개 장소를 드래그로 배치
4. 각 블록 체류시간 조정 → 타임라인에서 도착 시각이 실시간 갱신
5. 이동수단을 자동차 ↔ 대중교통으로 전환 → 소요시간과 총합이 바뀜
6. 타임라인 뷰 전환 → 블록 높이가 체류시간에 비례해 보임
7. "네이버지도로 열기" → 4개 지점이 출발지+경유지2+도착지로 들어감
8. 같은 코스를 다시 열어도 경로 API가 재호출되지 않음(캐시 적중)
여기까지 되면 나머지는 살 붙이기입니다.